# feed-current.json

This is a frozen copy of `.cache/feed-r3.json`.

Collector revision: `collector-run-2026-07-25T16:30:02.833Z` (the
`feed.source_revision` field in the file itself).

Offering count: 1163 (`len(models)`).

Profile count: 4 (`len(profiles)`).

Ticket 10 uses this file for the known-differences test. That test
asserts the live changes the spec already documents: Sunsetting
OpenCode Go Offerings, two Offerings Sunsetting on a deprecated status
alone, and the OpenRouter free router absent from the Feed.

Nothing may overwrite this file.
