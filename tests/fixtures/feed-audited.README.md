# feed-audited.json

This is a frozen copy of `.cache/feed-audited-2026-07-25T04.json`.

Collector revision: `collector-run-2026-07-25T04:24:25.512Z` (the
`feed.source_revision` field in the file itself).

Offering count: 1164 (`len(models)`).

This is the acceptance-test input for ticket 10. The `plan` transform,
given this snapshot, must reproduce the operator's Generated Config.

Nothing may overwrite this file. The live Feed has since changed, so a
fresh fetch would no longer match the expected config.
